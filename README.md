# Minimal Dependency Check Project

This is a minimal stub project to verify all dependencies compile and resolve correctly.

## Building

```bash
npm install
mvn clean install
npm run build-cds
```

## Running

```bash
npm start
```

## Notes

- All implementations are minimal stubs designed only for dependency validation
- Not intended as a functional application
- Use as reference before making changes to production repo
